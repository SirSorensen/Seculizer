Participants: Alice, Bob;
Knowledge: {
            Alice: X ? "This is an nonce", msgA ? "This is Alice's message";
            Bob: Y ? "This is a random number", msgB ? "This is Bob's message";
            Shared: g ? "This is a generator";
        };
Equations: exp(exp(g,Y),X) => exp(exp(g,X),Y);
Format: exp(x,y) = $x^y$;
Icons: {
    "woman": Alice;
    "man": Bob;
    "🔑": X, Y;
    "gear": g;
    "📝": msgA, msgB;
};  
Protocol: {
    Alice -> Bob: exp(g,X); ? "Alice sends Bob her public key"
    Bob -> Alice: exp(g,Y); ? "Bob sends Alice his public key"
    Alice -> Bob: {msgA}exp(exp(g,X),Y); ? "Alice sends Bob a message encrypted with Bob's public key"
    Bob -> Alice: {msgB}exp(exp(g,X),Y); ? "Bob sends Alice a message encrypted with Alice's public key"
};